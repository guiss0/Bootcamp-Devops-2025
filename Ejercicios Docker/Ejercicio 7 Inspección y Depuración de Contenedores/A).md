Qué comando te permite inspeccionar los detalles de configuración de un contenedor?

``` bash
docker inspect <contenedor>

docker inspect <contenedor> | grep -i "contenido a filtrar de la configuracion" 
```


